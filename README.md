# micro-frontend-galore

A repo-of-repos, contains multiple POC for micro-frontends:

- Vite Module Federation (runtime code sharing)
- Turbo Monorepo + React.Lazy and React.Suspense (build time code sharing)
